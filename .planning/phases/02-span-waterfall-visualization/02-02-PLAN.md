---
wave: 2
depends_on: ["02-01"]
files_modified:
  - dashboard/src/lib/api.ts
  - dashboard/src/components/traces/SpanWaterfall.tsx
  - dashboard/src/pages/TraceDetail.tsx
  - dashboard/src/App.tsx
autonomous: false
requirements_addressed: [TRAC-01, TRAC-02]
---

# Plan 02-02: Implement D3/Canvas or advanced nested rendering for the Waterfall UI

## Objective
Build the UI that displays the hierarchical span waterfall, complying with the user's constraint to use Custom HTML/CSS/Grid over heavy SVG libraries. Clicking a span must filter logs by exact `trace_id`.

## Tasks

<task>
<read_first>
- dashboard/src/lib/api.ts
</read_first>
<action>
Add `fetchTrace(traceId: string)` to `dashboard/src/lib/api.ts` that sends a POST to `/api/v1/traces/query`.
Define the `SpanResponse` interface export matching the backend structure (`span_id`, `parent_id`, `trace_id`, `name`, `service`, `timestamp`, `duration_ms`).
</action>
<acceptance_criteria>
- `grep "fetchTrace" dashboard/src/lib/api.ts` returns a match.
- `grep "parent_id" dashboard/src/lib/api.ts` returns a match.
</acceptance_criteria>
</task>

<task>
<read_first>
- dashboard/src/components/traces/SpanWaterfall.tsx
- .planning/phases/02-span-waterfall-visualization/02-CONTEXT.md
</read_first>
<action>
Create `dashboard/src/components/traces/SpanWaterfall.tsx`.
It must take a flat array of `SpanResponse` objects.
Write a helper function to convert the flat array into a nested tree using `parent_id`.
Render the tree using standard React recursive rendering mapped to CSS grid or flexbox rows with left-padding multipliers based on depth.
Render a visual block (e.g., `<div className="bg-blue-500 rounded h-4 absolute" style={{ left: startPercentage, width: widthPercentage }}></div>`) relative to the root span's total duration.
</action>
<acceptance_criteria>
- `grep "SpanWaterfall" dashboard/src/components/traces/SpanWaterfall.tsx` returns a match.
- `grep "style={{ left:" dashboard/src/components/traces/SpanWaterfall.tsx` returns a match.
</acceptance_criteria>
</task>

<task>
<read_first>
- dashboard/src/pages/TraceDetail.tsx
- dashboard/src/App.tsx
</read_first>
<action>
Create `dashboard/src/pages/TraceDetail.tsx`.
It should read `traceId` from the URL via `react-router-dom`'s `useParams`.
Fetch the trace spans on mount. Render the `<SpanWaterfall>` component.
Include a split-pane or bottom panel that renders the `<LogViewer />` component, automatically setting the search query to the `traceId` to satisfy TRAC-02.
Wire the route `/traces/:traceId` into `dashboard/src/App.tsx`.
</action>
<acceptance_criteria>
- `grep "useParams" dashboard/src/pages/TraceDetail.tsx` returns a match.
- `grep "LogViewer" dashboard/src/pages/TraceDetail.tsx` returns a match.
- `grep "/traces/:traceId" dashboard/src/App.tsx` returns a match.
</acceptance_criteria>
</task>

## Verification
- Start the app and navigate to `/traces/mock-123`.
- Ensure the waterfall renders as stepped horizontal bars.
- Ensure the logs panel below it explicitly shows logs related to `mock-123`.

## Must Haves
- Must use CSS Flex/Grid for rendering the waterfall, not D3.js.
- Must fulfill TRAC-02 by explicitly coupling the `LogViewer` to the active trace's ID.

---
wave: 2
depends_on: [52-01-PLAN.md]
files_modified:
  - agents/java/src/main/java/com/easymonitor/agent/HttpAdvice.java
  - agents/rust/easymonitor-agent/src/lib.rs
autonomous: true
gap_closure: false
---

# Plan 52-02: Java & Rust Profiling Contexts

<objective>
Adopt exact standard profiling bounds dynamically executing explicit CPU mapping parameters organically inside Java Servlets natively wrapping Java `ManagementFactory` bounds globally isolating optional Rust integrations cleanly.
</objective>

<requirements>
- Java MUST safely extract `ThreadMXBean.getCurrentThreadCpuTime` dynamically if available natively protecting against ClassNotFound vulnerabilities cleanly.
- Rust avoids heavyweight OS sys-calls strictly assigning execution counts organically mapping trace derivations functionally protecting Tokyo dispatchers globally.
</requirements>

## Tasks

### 1. Java JMX Span Injection
<read_first>
- `agents/java/src/main/java/com/easymonitor/agent/HttpAdvice.java`
- `agents/java/src/main/java/com/easymonitor/agent/ServletAdvice.java`
</read_first>
<action>
1. Update ByteBuddy interceptors exposing Java `ManagementFactory.getThreadMXBean()`.
2. Cache `cpuStart` thread ticks dynamically inside the standard Servlet `@Advice.OnMethodEnter`.
3. Inside `@Advice.OnMethodExit`, calculate delta limits `(cpuEnd - cpuStart) / 1000000` (ms) mapping `cpu.user` securely onto the active `DatadogSpan` payload natively.
4. Catch `UnsupportedOperationException` safely natively.
</action>

### 2. Rust Tracing Subscriber Overhead (Optional)
<read_first>
- `agents/rust/easymonitor-agent/src/lib.rs`
</read_first>
<action>
1. Acknowledge Rust OS mapping allocations safely matching the `tracing` standards dynamically mapping execution time naturally utilizing native `on_new_span` mapping structures appropriately mapping metadata variables without adding heavy libraries natively avoiding compile burdens.
</action>
<acceptance_criteria>
- Java successfully injects CPU thread durations directly mapping Datadog formats locally natively executing GC spans safely.
</acceptance_criteria>

---
wave: 1
depends_on: []
files_modified:
  - agents/node/instrumentation.ts
  - agents/go/telemetry.go
autonomous: true
gap_closure: false
---

# Plan 52-01: Node.js & Go Span Profiling

<objective>
Inject the Lightweight Profiling Span Processor extracting exact CPU tick and Heap allocations dynamically across Node and Go endpoints mapping OS impacts natively onto outgoing telemetry boundaries organically.
</objective>

<requirements>
- Avoid complex `.pprof` flamegraph integrations safely strictly maintaining Phase 52 minimum parameters tracking application overhead locally.
- In Node: Create `CpuMemProfilingProcessor` managing start/end structural limits utilizing standard `process` hooks.
- In Go: Attach `runtime.MemStats` diffing logic capturing allocation overhead natively wrapping contexts locally before exporting standard Telemetry structs.
</requirements>

## Tasks

### 1. Node.js Differential Processor
<read_first>
- `agents/node/instrumentation.ts`
</read_first>
<action>
1. Define `CpuMemProfilingProcessor` implementing OpenTelemetry's `SpanProcessor` organically mapping an internal `Map<string, {cpu, mem}>`.
2. Inside `onStart`: Store `process.cpuUsage()` and `process.memoryUsage().heapUsed` matching `span.spanContext().spanId`.
3. Inside `onEnd`: Subtract structural limits matching `process.cpuUsage(start_cpu)` mapping `cpu.user`, `cpu.system`, and `mem.delta` into `span.setAttribute()` dynamically.
4. Hook the custom processor securely alongside the `BatchSpanProcessor` natively.
</action>

### 2. Go Differential Extraction
<read_first>
- `agents/go/telemetry.go`
</read_first>
<action>
1. Update `telemetry.StartSpan` dynamically appending memory allocation baselines cleanly.
2. On `span.End()`, call `runtime.ReadMemStats` natively validating `TotalAlloc` structural differences explicitly into the struct `metrics` dictionary effectively guaranteeing GC allocations explicitly.
</action>
<acceptance_criteria>
- CPU bounds map onto Node traces natively securely isolating metric attributes dynamically.
- Core agents compile successfully without heavy dependency impacts.
</acceptance_criteria>

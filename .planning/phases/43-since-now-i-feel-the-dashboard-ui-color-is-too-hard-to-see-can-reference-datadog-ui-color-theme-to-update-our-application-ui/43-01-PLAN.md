---
wave: 1
depends_on: []
files_modified:
  - dashboard/src/index.css
  - dashboard/tailwind.config.js
  - dashboard/src/components/layout/ThemeContext.tsx
  - dashboard/src/App.tsx
  - dashboard/src/components/layout/UserMenu.tsx
  - dashboard/src/components/layout/Sidebar.tsx
  - dashboard/src/pages/Logs.tsx
  - dashboard/src/components/logs/LogViewer.tsx
  - dashboard/src/components/logs/LogDetailPanel.tsx
autonomous: false
---

# Phase 43: Datadog UI Theme Update - Plan 01

## <objective>
Fully overhaul the dashboard's Tailwind theme to properly support Datadog-style Dark and Light modes. Establish a ThemeContext for toggling, update `index.css` and `tailwind.config.js` to rely on CSS variables for primary structural colors, and refactor layout & log components from static `gray-*` classes to semantic theme classes.
</objective>

## <tasks>

<task>
<id>43-01-01</id>
<title>Setup Global Theme CSS and Context</title>
<read_first>
- `dashboard/src/index.css`
- `dashboard/src/App.tsx`
</read_first>
<action>
1. Edit `dashboard/src/index.css` to define CSS variables for the color palette inside `@layer base`.
   ```css
   @layer base {
     :root {
       --color-bg-base: #FFFFFF;
       --color-bg-surface: #F3F4F5;
       --color-bg-surface-light: #E0E1E4;
       --color-text-primary: #191B28;
       --color-text-inverse: #FFFFFF;
       --color-text-secondary: #4F5568;
       --color-text-muted: #8E93A3;
       --color-border: #D0D5DD;
     }

     .dark {
       --color-bg-base: #151821;
       --color-bg-surface: #1E2130;
       --color-bg-surface-light: #2A2E3F;
       --color-text-primary: #E6E8EB;
       --color-text-inverse: #191B28;
       --color-text-secondary: #8A94A6;
       --color-text-muted: #5F6B7A;
       --color-border: #2A2E3F;
     }
     /* Keep existing HTML/body tags but update to use CSS variables if needed */
   ```
2. Create `dashboard/src/components/layout/ThemeContext.tsx` to manage dark mode state.
   It should export a `ThemeProvider` that reads `localStorage.getItem('theme')`, sets `document.documentElement.classList.add('dark')` accordingly, and provides a `toggleTheme` function.
3. Update `dashboard/src/App.tsx` to wrap the routing completely in `<ThemeProvider>`.
</action>
<acceptance_criteria>
- `dashboard/src/index.css` contains `:root` and `.dark` variable definitions.
- `eslint dashboard/src/components/layout/ThemeContext.tsx` exits 0.
- `dashboard/src/App.tsx` imports and uses `ThemeProvider`.
</acceptance_criteria>
</task>

<task>
<id>43-01-02</id>
<title>Update Tailwind Configuration</title>
<read_first>
- `dashboard/tailwind.config.js`
</read_first>
<action>
Modify `dashboard/tailwind.config.js`:
1. Keep `darkMode: 'class'`.
2. Under `theme.extend.colors`, replace the hardcoded hexes for `background`, `surface`, `text`, and `border` with the CSS variables.
   ```javascript
   background: 'var(--color-bg-base)',
   surface: {
     DEFAULT: 'var(--color-bg-surface)',
     light: 'var(--color-bg-surface-light)',
   },
   text: {
     primary: 'var(--color-text-primary)',
     inverse: 'var(--color-text-inverse)',
     secondary: 'var(--color-text-secondary)',
     muted: 'var(--color-text-muted)',
   },
   border: {
     DEFAULT: 'var(--color-border)',
   },
   ```
</action>
<acceptance_criteria>
- `dashboard/tailwind.config.js` properly references `var(--color-bg-base)`.
</acceptance_criteria>
</task>

<task>
<id>43-01-03</id>
<title>Refactor Layout and Introduce Toggle</title>
<read_first>
- `dashboard/src/components/layout/UserMenu.tsx`
- `dashboard/src/components/layout/MainLayout.tsx`
- `dashboard/src/components/layout/Sidebar.tsx`
</read_first>
<action>
1. In `UserMenu.tsx` (or `Sidebar.tsx`, whichever fits best), add a Theme Toggle button. Use `useTheme` from `ThemeContext`, render a `Sun` icon when dark, and `Moon` icon when light.
2. Ensure `MainLayout.tsx` uses `bg-background text-text-primary` natively.
3. Replace hardcoded standard Tailwind colors in `Sidebar.tsx` with semantic tokens (`bg-surface`, `text-text-primary`, `hover:bg-surface-light`).
</action>
<acceptance_criteria>
- `UserMenu.tsx` or `Sidebar.tsx` imports `Sun`, `Moon` from `lucide-react`.
- Uses `useTheme` to toggle `isDark`.
- `eslint dashboard/src/components/layout/UserMenu.tsx` exits 0.
</acceptance_criteria>
</task>

<task>
<id>43-01-04</id>
<title>Refactor Logs UI components to Semantic Themes</title>
<read_first>
- `dashboard/src/pages/Logs.tsx`
- `dashboard/src/components/logs/LogViewer.tsx`
- `dashboard/src/components/logs/LogDetailPanel.tsx`
</read_first>
<action>
1. Systematically replace fixed gray classes with our semantic palette in `Logs.tsx`:
   - `bg-gray-950` -> `bg-background`
   - `bg-gray-900` -> `bg-surface`
   - `bg-gray-800` -> `bg-surface-light` or `border-border`
   - `text-gray-400`, `text-gray-500` -> `text-text-secondary`
2. Update `LogViewer.tsx`:
   - `bg-blue-500/10` -> `bg-brand-glow`
   - `border-gray-800/40` -> `border-border`
   - `text-gray-300` -> `text-text-primary`
   - Verify `hover:bg-white/5` handles light mode well (or replace with `hover:bg-surface-light`).
3. Update `LogDetailPanel.tsx`:
   - `bg-gray-900/40` -> `bg-surface`
   - Fixed borders -> `border-border`
   - Internal grid background (e.g., `bg-black/20`) -> `bg-surface-light`/`bg-background`.
</action>
<acceptance_criteria>
- Fixed `bg-gray-950` is removed from `Logs.tsx`.
- Semantic theme variables correctly applied in `Logs.tsx` and `LogViewer.tsx`.
- `npm run lint` clears with 0 errors.
</acceptance_criteria>
</task>

</tasks>

## <verification>
<must_haves>
- Tailwind config correctly processes CSS variables safely without CSS parse errors.
- Light and Dark modes switch instantly when the toggle is clicked.
- All text in Logs and Layout remains legible in both themes, strictly following Datadog's high-contrast accessibility.
- The React frontend builds and lints successfully.
</must_haves>
</verification>

---
phase: 46
plan: 02
wave: 1
depends_on: []
files_modified:
  - agents/rust/easymonitor-agent/src/lib.rs
  - agents/rust/easymonitor-agent/src/reqwest_middleware.rs
  - agents/rust/easymonitor-agent/src/actix_middleware.rs
autonomous: true
---

# Phase 46 - Plan 02: Rust Agent Trace Generation & Context Propagation

## Objective
Implement native Span lifecycle management (Start, End) and distributed trace header propagation (`x-easymonitor-*`) in the Rust agent.

## Tasks

```xml
<task>
  <description>Implement Rust Span Lifecycle & Header Variables</description>
  <read_first>
    - agents/rust/easymonitor-agent/src/lib.rs
  </read_first>
  <action>
    Modify `agents/rust/easymonitor-agent/src/lib.rs`:
    1. Define constants for headers:
       ```rust
       pub const HEADER_TRACE_ID: &str = "x-easymonitor-trace-id";
       pub const HEADER_PARENT_ID: &str = "x-easymonitor-parent-id";
       pub const HEADER_SAMPLING_PRIORITY: &str = "x-easymonitor-sampling-priority";
       ```
    2. Add `rand` to dependencies if not present, and implement a `generate_id() -> u64` function.
    3. Ensure `SpanData` captures `Option<u64>` for `parent_id` and calculates duration via `SystemTime::now().duration_since(UNIX_EPOCH).unwrap().as_nanos() as i64`. The existing `tracing` layer naturally handles `on_new_span` and `on_close`. Ensure IDs are randomly generated inside `on_new_span` if no parent is present, using `generate_id()`.
  </action>
  <acceptance_criteria>
    - `agents/rust/easymonitor-agent/src/lib.rs` contains `pub const HEADER_TRACE_ID`.
    - `agents/rust/easymonitor-agent/src/lib.rs` uses `as_nanos()` for time.
  </acceptance_criteria>
</task>

<task>
  <description>Implement Rust Context Extraction (Actix)</description>
  <read_first>
    - agents/rust/easymonitor-agent/src/actix_middleware.rs
  </read_first>
  <action>
    Modify `agents/rust/easymonitor-agent/src/actix_middleware.rs`:
    1. Intercept incoming requests and check `req.headers().get("x-easymonitor-trace-id")` and `"x-easymonitor-parent-id"`.
    2. Parse the string value using `.to_str().unwrap().parse::<u64>()`.
    3. Inject these extracted IDs into the `tracing` span context using `tracing::info_span!` fields, or a thread-local storage / request-local storage so the `tracing::Layer` can read them to set the root `TraceID` and `ParentID`.
  </action>
  <acceptance_criteria>
    - `agents/rust/easymonitor-agent/src/actix_middleware.rs` reads `x-easymonitor-trace-id` from headers.
    - Successfully parses header to `u64`.
  </acceptance_criteria>
</task>

<task>
  <description>Implement Rust Context Injection (Reqwest)</description>
  <read_first>
    - agents/rust/easymonitor-agent/src/reqwest_middleware.rs
  </read_first>
  <action>
    Modify `agents/rust/easymonitor-agent/src/reqwest_middleware.rs`:
    1. Impl `reqwest_middleware::Middleware`.
    2. Retrieve the active tracing Span ID and Trace ID (this may require retrieving it from `tracing::Span::current()` via extensions or checking locally stored variables).
    3. Insert headers: `req.headers_mut().insert("x-easymonitor-trace-id", HeaderValue::from_str(&trace_id.to_string()).unwrap());`
    4. Insert `x-easymonitor-parent-id` using the current span's ID.
  </action>
  <acceptance_criteria>
    - `reqwest_middleware.rs` modifies `req.headers_mut().insert` with `x-easymonitor-trace-id`.
  </acceptance_criteria>
</task>
```

## Verification
- Run `cargo check --manifest-path ./agents/rust/easymonitor-agent/Cargo.toml` or `cargo test`.

---
wave: 1
depends_on: []
files_modified:
  - dashboard/package.json
  - dashboard/src/App.tsx
  - dashboard/src/components/layout/Sidebar.tsx
  - dashboard/src/components/layout/MainLayout.tsx
  - dashboard/src/hooks/useTimeRange.ts
autonomous: true
requirements_addressed: [DASH-01, INGST-01, INGST-02]
---

# Plan 01-01: Establish Dashboard Layout and Routing

## Objective
Set up the React Router foundation, URL state management for global time, and the side navigation layout framework to support later components. Incorporate backend basic health endpoints if needed to fulfill basic ingestion checks.

## Tasks

<task>
<read_first>
- dashboard/package.json
- .planning/phases/01-unified-dashboard-foundation/01-CONTEXT.md
</read_first>
<action>
Install the required dependencies for routing, virtualization, charting, and icons. Add `react-router-dom`, `lucide-react`, `date-fns`, `recharts`, and `react-virtuoso` to `dashboard/package.json`.
Run `npm install react-router-dom lucide-react date-fns recharts react-virtuoso` inside the `dashboard/` directory.
</action>
<acceptance_criteria>
- `cat dashboard/package.json | grep react-router-dom` returns a match.
- `cat dashboard/package.json | grep lucide-react` returns a match.
- `cat dashboard/package.json | grep recharts` returns a match.
</acceptance_criteria>
</task>

<task>
<read_first>
- dashboard/src/hooks/useTimeRange.ts
- .planning/phases/01-unified-dashboard-foundation/01-RESEARCH.md
</read_first>
<action>
Create a custom hook `dashboard/src/hooks/useTimeRange.ts` that wraps `react-router-dom`'s `useSearchParams`.
It must export `{ from, to, setTimeRange }`.
If the URL lacks `from` or `to` parameters, it should default `from` to `now-1h` and `to` to `now`.
It must use `setSearchParams` to update the URL so the state remains shareable.
</action>
<acceptance_criteria>
- `cat dashboard/src/hooks/useTimeRange.ts | grep useSearchParams` returns a match.
- `cat dashboard/src/hooks/useTimeRange.ts | grep "from.*now-1h"` returns a match.
</acceptance_criteria>
</task>

<task>
<read_first>
- dashboard/src/App.tsx
- dashboard/src/components/layout/MainLayout.tsx
- dashboard/src/components/layout/Sidebar.tsx
</read_first>
<action>
Create `dashboard/src/components/layout/Sidebar.tsx` with links to `/` (Metrics) and `/logs` (Logs) using `lucide-react` icons (e.g., `Activity` and `List`).
Create `dashboard/src/components/layout/MainLayout.tsx` which includes the `Sidebar` on the left and an `<Outlet />` on the right for page content.
Rewrite `dashboard/src/App.tsx` to set up `BrowserRouter` with a `Routes` block.
Define a `/` route pointing to a dummy `<div>Metrics</div>` inside `MainLayout`.
Define a `/logs` route pointing to a dummy `<div>Logs</div>` inside `MainLayout`.
</action>
<acceptance_criteria>
- `grep "BrowserRouter" dashboard/src/App.tsx` returns a match.
- `grep "Sidebar" dashboard/src/components/layout/MainLayout.tsx` returns a match.
- `grep "Activity" dashboard/src/components/layout/Sidebar.tsx` returns a match.
</acceptance_criteria>
</task>

## Verification
- Run `npm run dev` in `dashboard/` and ensure the app loads without errors.
- Ensure clicking between Sidebar nav items updates the URL to `/` and `/logs`.
- Ensure navigating to `/?from=now-5m` retains the query parameter without breaking the layout.

## Must Haves
- `react-router-dom` successfully controls rendering.
- `useTimeRange` correctly parses URL parameters.

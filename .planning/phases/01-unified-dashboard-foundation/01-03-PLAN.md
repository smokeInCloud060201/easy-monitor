---
wave: 2
depends_on: ["01-01"]
files_modified:
  - dashboard/src/pages/Logs.tsx
  - dashboard/src/components/logs/LogViewer.tsx
  - dashboard/src/lib/api.ts
  - master-service/src/api/logs.rs
autonomous: false
requirements_addressed: [DASH-02, DASH-03]
---

# Plan 01-03: Implement Log searchable data table and global time context

## Objective
Build the high-performance virtualized log tailing viewer. Connect it to the backend via SSE (Server-Sent Events) or REST, ensuring it respects the same global URL time filter from `useTimeRange`.

## Tasks

<task>
<read_first>
- master-service/src/api/logs.rs
- master-service/src/main.rs
</read_first>
<action>
Create or modify `master-service/src/api/logs.rs` to expose a REST endpoint at `/api/logs`.
It must accept `from`, `to`, and an optional `query` string param.
For MVP, if there is no live sled index for logs yet, generate and return an array of 500 mock log objects (timestamp, level, message, service_name).
Register this route in the `axum` router in `master-service/src/main.rs`.
</action>
<acceptance_criteria>
- `grep "/api/logs" master-service/src/api/logs.rs` or `master-service/src/main.rs` returns a match.
- Code shows parsing of `query`, `from`, `to` parameters.
</acceptance_criteria>
</task>

<task>
<read_first>
- dashboard/src/lib/api.ts
- dashboard/src/components/logs/LogViewer.tsx
- .planning/phases/01-unified-dashboard-foundation/01-RESEARCH.md
</read_first>
<action>
Add `fetchLogs(from: string, to: string, query: string)` to `dashboard/src/lib/api.ts`.
Create `dashboard/src/components/logs/LogViewer.tsx` using `react-virtuoso`.
It must export a component that accepts a `logs` array prop.
Render the logs using `<Virtuoso followOutput="smooth" ... />` to ensure high performance and auto-scrolling behavior for tailing.
Format each log row cleanly: `[Timestamp] [Level/Color] [Message]`.
</action>
<acceptance_criteria>
- `grep "fetchLogs" dashboard/src/lib/api.ts` returns a match.
- `grep "Virtuoso" dashboard/src/components/logs/LogViewer.tsx` returns a match.
- `grep "followOutput" dashboard/src/components/logs/LogViewer.tsx` returns a match.
</acceptance_criteria>
</task>

<task>
<read_first>
- dashboard/src/pages/Logs.tsx
- dashboard/src/App.tsx
</read_first>
<action>
Create `dashboard/src/pages/Logs.tsx`.
Use the `useTimeRange` hook to parse `from` and `to`.
Add a text `<input>` for users to type a basic search string (using local state).
In a `useEffect` triggered by `from`, `to`, or the debounced search string, call `fetchLogs` and store the result.
Render the `<LogViewer />` with the results.
Update `dashboard/src/App.tsx` to map `/logs` to `<Logs />`.
</action>
<acceptance_criteria>
- `grep "useTimeRange" dashboard/src/pages/Logs.tsx` returns a match.
- `grep "<input" dashboard/src/pages/Logs.tsx` returns a match.
- `grep "LogViewer" dashboard/src/pages/Logs.tsx` returns a match.
</acceptance_criteria>
</task>

## Verification
- Navigate to `/logs`.
- Ensure the virtualized log list mounts without crashing the browser.
- Typing in the search input triggers `fetchLogs` and filters the view.

## Must Haves
- Virtualization via `react-virtuoso` is strictly enforced to prevent DOM bloat.
- Dashboard URL state controls the log querying window.

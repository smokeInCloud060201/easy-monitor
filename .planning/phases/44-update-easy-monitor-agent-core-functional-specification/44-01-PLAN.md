---
wave: 1
depends_on: []
files_modified:
  - ".planning/ROADMAP.md"
  - "node-agent/Cargo.toml"
  - "node-agent/src/apm/http_receiver.rs"
  - "node-agent/src/apm/mod.rs"
autonomous: true
---

# Plan 44-01: Node Agent Trace Receiver and Roadmap Evolution

## Objective
Update the `ROADMAP.md` to formally schedule the remaining 13 modules of the Agent Core Functional Specification. Then, extend the `node-agent` Rust application to listen for HTTP POST `/v0.4/traces` and successfully deserialize MessagePack payloads into the WAL.

## Tasks

```xml
<task>
  <description>Update the project Roadmap</description>
  <action>
    Modify `.planning/ROADMAP.md` to append future phases for the remaining 13 Agent modules (e.g., Trace Generation, Metrics Aggregation, Error Collection) so nothing is missed.
  </action>
  <read_first>
    - .planning/ROADMAP.md
    - agents/README.md
  </read_first>
  <acceptance_criteria>
    - `grep -q "Phase 45" .planning/ROADMAP.md` exits 0.
    - `grep -q "Metrics Aggregation" .planning/ROADMAP.md` exits 0.
  </acceptance_criteria>
</task>

<task>
  <description>Implement node-agent `/v0.4/traces` HTTP MessagePack Receiver</description>
  <action>
    Add `axum` or `actix-web` and `rmp-serde` (MessagePack) to `node-agent/Cargo.toml`. Create `node-agent/src/apm/http_receiver.rs` to expose an HTTP POST `/v0.4/traces` endpoint. 
    The handler should accept a `bytes::Bytes` payload, use `rmp_serde::from_slice` to deserialize it into `Vec<Vec<DatadogSpan>>`, translate each to `shared_proto::traces::Span`, and write it to the WAL via `self.wal.write_trace()`.
  </action>
  <read_first>
    - node-agent/Cargo.toml
    - node-agent/src/apm/mod.rs
    - .planning/phases/44-update-easy-monitor-agent-core-functional-specification/44-RESEARCH.md
  </read_first>
  <acceptance_criteria>
    - `cargo check --manifest-path node-agent/Cargo.toml` exits 0.
    - `cat node-agent/src/apm/http_receiver.rs | grep "/v0.4/traces"` exits 0.
  </acceptance_criteria>
</task>
```

## Verification
1. `cargo test --manifest-path node-agent/Cargo.toml`
2. Run `node-agent` and send a test `curl` with a MessagePack payload to ensure it returns `200 OK`.
